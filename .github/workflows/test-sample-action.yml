on:
  workflow_dispatch:

jobs:
  sync-milestones:
    runs-on: ubuntu-latest
    name: Sync Milestones 
    steps:
      - name: Copy Milestones 
        id: copy-milestones
        uses: deanhigh/sample-action@main
        with:
          repositories: 'deanhigh/test-sample-action,deanhigh/sample-action'
          token: ${{ secrets.CROSS_REPO_PAT }}
